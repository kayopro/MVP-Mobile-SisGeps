name: SisGeps
description: Sistema móvel para gerenciamento de estoque.

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.0

flutter:
  assets:
    - assets/images/logo.png
